<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="{{ path('homepage') }}">Cocktails Party</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobileMenu" aria-controls="mobileMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="mobileMenu">
            <ul class="navbar-nav ml-auto mt-2 mt-lg-0">
                <ul id="nav-mobile" class="right hide-on-med-and-down">
                    {% if app.user %}
                        <li><a class="nav-link" href="/logout">Logout</a></li>
                        <a class="nav-link" href="{{ path('creat') }}">Créer un cocktail</a>
                    {% else %}
                        <li>  <a class="nav-link" href="{{ path('app_register') }}">Connexion</a></li>
                    {% endif %}
                </ul>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('homepage') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('cart_index') }}">Panier</a>
                </li>
            </ul>
        </div>
    </nav>
</header>